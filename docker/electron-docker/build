#!/bin/bash
set -x
chmod 755 entrypoint
git clone https://github.com/electron/electron-quick-start app || true
docker build \
	--build-arg UID=$(id -u) \
	--build-arg GID=$(id -g) \
	-t electron-docker \
	-f Dockerfile \
	.
